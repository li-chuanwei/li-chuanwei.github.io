## 1. 首屏与主题行为

- [x] 1.1 删除首屏纵向滚动提示节点和关联样式；使用文本与选择器检索确认无残留。
- [x] 1.2 将无偏好访问者的默认主题设为深色并保留已保存的浅色偏好；在浏览器中确认根主题和背景色。
- [x] 1.3 同步浏览器主题色并避免首次绘制闪白；检查 HTML 初始主题与主题切换逻辑一致。

## 2. 导航栏滚动交互

- [x] 2.1 使用被动滚动监听在约 80px 阈值切换导航状态；检查阈值前后状态类。
- [x] 2.2 实现顶部透明、滚动后边框和玻璃背景淡入；通过浏览器截图和计算样式验证。
- [x] 2.3 实现桌面端约 140px 的平滑内收并保持导航吸顶；测量起始、中间和结束宽度及顶部位置。
- [x] 2.4 保留减少动态效果支持；确认既有媒体查询覆盖新增过渡。

## 3. 上下导航尺寸与质量检查

- [x] 3.1 统一桌面端上下导航为 1040px 宽、50px 高；在 1280px 视口测量验证。
- [x] 3.2 统一移动端上下导航宽度并设为 48px 高；在 390px 视口测量验证。
- [x] 3.3 运行 `pnpm.cmd lint`、`pnpm.cmd build` 与 `git diff --check`；确认全部通过。
